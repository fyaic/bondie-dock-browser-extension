# Handoff 2026-06-18

## 当前分支

```text
repo: /Users/fuyo-aic/Projects/openclaw-browser-host-extension
branch: feature/bondie-multi-instance-permissions
latest validated implementation commit: b269c6f
```

Session Bridge 依赖仓库：

```text
repo: /Users/fuyo-aic/Projects/openclaw-session-bridge
branch: main
latest local fix: 6b31801 Fix exact route session lookup timeout
status: clean, ahead 1 at last check
```

## 今天已完成

### Phase 7C Control Plane contract

- `8cf3ae3 Add Bondie control plane contract helpers`
- `75cebda Add control plane contract smoke test`
- `9b3fe96 Add Bondie control plane adapter skeleton`
- `43e6031 Add side panel oauth fail-closed smoke`
- `bb51a19 Add control plane health projection contract`
- `d4ff262 Add control plane readiness negative tests`
- `df6df78 Add side panel OAuth token contract`
- `b269c6f Normalize side panel OAuth token stub`

产物：

- `extension/src/modules/openclaw-side-panel/control-plane-contract.js`
- `extension/src/modules/openclaw-side-panel/control-plane-adapter.js`
- `extension/src/modules/openclaw-side-panel/oauth-token-contract.js`
- `scripts/test-control-plane-contract.mjs`
- `docs/side-panel-browser-plugin-harness/12-control-plane-contract.md`

已固定：

- OAuth 未完成时 `identity_required`，仅 device pairing 不列 sessions。
- Control Plane URL 缺失时 `permission_unresolved`。
- 缺 access token 时不发请求。
- `subordinate/all_sessions` 与 `communication/participant_sessions` 必须显式匹配。
- 无效 relationship/policy 被过滤。
- unknown/invalid instance 不 fallback legacy/global route。
- new/switch 只认 explicit confirmation。
- health projection 保留摘要字段，但不暴露 endpoint/token/debug raw。
- degraded/offline instance 默认禁用 action，除非服务端显式 `actions_enabled=true`。
- 401、disabled action、stale health 均有 repeatable negative tests。
- OAuth token state 已有 provider-neutral normalizer，background stub 默认 `provider_unconfigured`。

### Phase 8 Bridge registry and device distribution

- `9243494 Document Bondie bridge registry onboarding`
- `a2bd615 Document multi bridge health readiness`

产物：

- `docs/side-panel-browser-plugin-harness/13-bridge-registry-schema.md`
- `docs/side-panel-browser-plugin-harness/14-device-onboarding-checklist.md`
- `docs/side-panel-browser-plugin-harness/15-multi-bridge-health-readiness.md`
- `docs/side-panel-browser-plugin-harness/17-oauth-token-interface.md`

已固定：

- `bondie_instances`
- `user_instance_relationships`
- `bridge_devices`
- `bridge_secrets`
- `instance_bridge_bindings`
- `bridge_health_snapshots`
- onboarding 流程：OpenClaw/Gateway 预检、Tailscale/private network、Session Bridge 安装、launchd/systemd、Control Plane 侧 smoke、registry 写入、Browser Side Panel 验收、回滚。
- multi-bridge health/readiness：online/degraded/offline/unauthorized/misconfigured，single primary，standby 不自动 failover。

## 已验证

```bash
node --check "extension/src/background.js"
node --check "extension/src/modules/openclaw-side-panel/control-plane-adapter.js"
node --check "extension/src/modules/openclaw-side-panel/control-plane-contract.js"
node --check "extension/src/modules/openclaw-side-panel/module.js"
node --check "scripts/test-control-plane-contract.mjs"
node "scripts/test-control-plane-contract.mjs"
python3 -m json.tool "extension/manifest.json" >/dev/null
git diff --check
./scripts/package-extension.sh
```

关键 smoke 输出：

```json
{"ok":true,"instances":2,"sessions":1,"adapterCalls":4,"moduleFailClosed":true}
```

扩展打包产物：

```text
dist/openclaw-browser-host-extension-0.1.0-alpha.12.zip
```

## 仍未完成

### 需要重要决策

- OAuth provider：OpenClaw 账号、AIC 账号，还是独立身份服务。
- Control Plane 服务端仓库和部署位置。
- bridge secret store：环境变量、Vault、云 secret manager，或先用受限数据库引用。
- 已创建 private Session Bridge 远程仓库 `https://github.com/fyaic/bondie-openclaw-session-bridge.git`；`main` 已推送到 `06ea5f5`。

### 可以继续闭环

- 给 `BondieControlPlaneAdapter` 增加 bridge_id mismatch / misconfigured fake-fetch test。
- 准备 `module.js` runtime wiring，但默认仍保持 `bondie-control-plane` fail closed，直到 token provider 可用。
- 补 Chrome for Testing 回归脚本入口，把 `scripts/test-control-plane-contract.mjs` 纳入常规检查列表。
- 按 `18-system-boundary-and-repo-plan.md` 继续维护三仓互索引文档，远程建仓前只做本地文档和实现准备。

## 禁止回退

- 不把 bridge endpoint/token 放进浏览器 Options 作为多 Bondie 方案。
- 不用 device pairing 代替 OAuth user identity。
- 不在 Control Plane adapter 失败时 fallback 到 legacy Session Bridge。
- 不用 URL、route label、手写 instanceId 提升权限。
- 不用 HTTP 200 或 `ok=true` 判断 new/switch 业务完成。
- 不把 Bondie Control Plane 当作浏览器 Side Panel 前端；Control Plane 是服务侧 API / 控制面。

## Linear 同步

已同步：

- `AIC-3043`: Bondie 多实例 Side Panel 权限与 UI。
- `AIC-3044`: Bondie Control Plane 与 Bridge Registry。

`AIC-3045` 的 Session Bridge 504 修复已完成源码和正式服务 smoke，后续只需要按发布节奏处理远端同步。
